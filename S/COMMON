dir="$(realpath "$(dirname "$0")")"
prog="$(basename "$0")"
repeat()
{
	printf %"$1"s | tr ' ' "$2"
}
getshpid()
{
grep PPid: /proc/"$$"/status | awk '{print $2}'
}
[ "${TERMUX_VERSION+x}" ] && in_termux=1 || in_termux=0
